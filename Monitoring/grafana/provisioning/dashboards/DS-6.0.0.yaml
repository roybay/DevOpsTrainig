apiVersion: 1

providers:
- name: 'Directory Services Dashboard'
  orgId: 1
  folder: 'ForgeRock'
  type: file
  disableDeletion: true
  updateIntervalSeconds: 1 #how often Grafana will scan for changed dashboards
  options:
    path: /etc/grafana/provisioning/dashboards/ds-topology.json